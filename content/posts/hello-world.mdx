# Hello World

这是新的基于文件系统内容层的第一篇文章。只要这个文件存在于 `content/posts` 目录下，它就会自动显示在首页上。

## 为什么选择这种方案

文件系统是唯一的数据源，发布文章就像添加或删除一个 `.mdx` 文件一样简单。

```tsx
export function HelloWorld() {
  return <p>上传即发布。</p>;
}
```
